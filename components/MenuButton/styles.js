import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        paddingTop: 10,
        paddingBottom: 20,
        width: '100%'
    },
    button: {

    },
    icon: {
        width: 60,
        height: 60,
        resizeMode: 'contain',
        padding: 30,
    }
});

export default styles;